import { Story, Meta } from '@storybook/react'
import Connection from '.'

export default {
  title: 'Connection',
  component: Connection
} as Meta

export const Default: Story = () => <Connection />
